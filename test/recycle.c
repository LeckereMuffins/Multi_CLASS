  
  // HALO MASS FUNCTION CALCULATION //
  double halo_mass_fct;       //calculate the SFR using the best-fit from Tinker et al.: arXiv:0803.2706v1
  double sigma;               //standard deviation
  double f_sigma;             //deviations from linear collapse - f(sigma)
  double amplitude = 0.248;   //amplitude of f(sigma), parameters for delta = 800
  double a_param = 1.87;      //parameters of f(sigma) - a, b, c
  double b_param = 1.59;
  double c_param = 1.58;

  //f_sigma = amplitude*(())

